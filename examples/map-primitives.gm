m = Map();
m.set("das", "das!");
m.set("ist", "ist!");
m.set("ein", "ein!");
m.set("test", "Test!");
m.set("number", 1.23);
m.set("das", null);

foreach(e in m) {
	log("- " + e);
}

log(m.das);
log(m["das"]);
log(""+m.number);
log(""+m["na"]);
